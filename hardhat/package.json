{
  "name": "cipherstream-hardhat",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Hardhat workspace for CipherStream smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network arbitrumSepolia",
    "verify": "hardhat verify --network arbitrumSepolia"
  },
  "devDependencies": {
    "hardhat": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "latest",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.0"
  }
}
